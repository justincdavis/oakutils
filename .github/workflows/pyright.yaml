name: Pyright
on: [ push ]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Pyright
        uses: jakebailey/pyright-action@v2
        with:
          version: latest
          project: pyproject.toml
